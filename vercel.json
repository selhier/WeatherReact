{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/locales/(.*)",
            "dest": "/locales/$1"
        },
        {
            "src": "/locales/en.json",
            "dest": "/locales/en.json"
        }
    ]
}
